# Acceptance Matrix — current outcome → implementation proof → real-company validation → Calvin acceptance

**OUTCOME-ID:** `CF-ACCEPTANCE-MATRIX-01`. Finish-line item (1) of Calvin's
21 Sep 2026 plan, per issue [#202](https://github.com/calyeap/Cal-Finance/issues/202)'s
closing instruction. This file **records** current truth; it is not a
source of truth of its own. It subordinates itself to the authorities
[`docs/CURRENT-AUTHORITY.md`](CURRENT-AUTHORITY.md) already names, and to
[`AGENTS.md`](../AGENTS.md)'s precedence rule. It grants, claims or implies
nothing beyond what is cited: it does **not** grant Calvin's final
live-product acceptance of M9, does not resolve blocker 2 (the M8 growth
comparator) or design-contract §8 items 2/4, and does not reopen the
18 Sep 2026 verdict-boundary ruling (row 13) or Calvin's three 21 Sep
rulings (rows 5, 10, and the confidence half of row 14's own history).

Rows are #118's BOUNDED RUNWAY items 1–12 (issue
[#118](https://github.com/calyeap/Cal-Finance/issues/118), closed 21 Sep
2026 by PRs #169/#200 as a GitHub side-effect — its runway text still
governs per issue #205's AUTHORITY), plus the two acceptance-relevant
blockers `docs/m9-acceptance-record.md` names that don't map to a single
runway item. Evidence re-read at `origin/master` = `2c805b4`
(2026-09-21, `CF-REALRUN-CURRENT-01`) unless a row states otherwise; rows 5
and 10's REAL-COMPANY VALIDATION cells were the ones this re-read actually
touched — every other row's citation is unchanged from the prior
`736e773` read.
IMPLEMENTATION PROOF cites file/line, test name, merged-PR SHA, or CI —
never a PR body's own claim.

| OUTCOME | IMPLEMENTATION PROOF | REAL-COMPANY VALIDATION | CALVIN ACCEPTANCE | STATUS |
|---|---|---|---|---|
| **1. M9 contract reconciliation** (#118 item 1) | PR #149, `M9-CONTRACT-RECON-01` (`0a0f11c`, merged 2026-09-18T08:24:14Z) — `docs/design/m9-contract-reconciliation.md`, the verdict/failure/confidence boundary later runway items build from. | — (documentation/reconciliation outcome; no product surface to run a real company against). | NOT YET — Calvin's gate. (The 15 Sep 2026 #118 authorisation covers the M9 initiative generally, not a per-item acceptance of this reconciliation.) | IMPLEMENTED. |
| **2. M9 design contract** (#118 item 2) | PR #152, `M9-DESIGN-CONTRACT-01` (`a770e30`, merged 2026-09-18T13:15:15Z) — `docs/design/m9-analyzer-design-contract.md`. | — (design/IA document; nothing executable to validate against a real run). | NOT YET — Calvin's gate. | IMPLEMENTED. §8 leaves 4 items open (row 14 below tracks the two still-open ones). |
| **3. Desktop shell** (#118 item 3) | PR #159, `M9-DESKTOP-SHELL-01` (`830b058`, merged 2026-09-19T04:00:38Z) — `app/components/AnalyzerOverview.tsx`, `ScenarioRangeStrip.tsx`; twelve-slot fixed order asserted by `app/components/AnalyzerOverview.test.tsx` (cited `docs/m9-acceptance-record.md` §1). | `docs/m9-real-company-validation-findings.md` §2–§3 — both MSFT and OKLO render all twelve Overview slots, none absent, on the real acquired path. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **4. Overview implementation** (#118 item 4) | PR #161 `M9-OVERVIEW-CONTENT-01` (`909bc5b`, merged 2026-09-19T04:37:38Z) + PR #163 `M9-OVERVIEW-CHALLENGER-COPY-01` (`62d7dbb`, merged 2026-09-19T04:57:58Z) — slots 6–10 sourced editorial content. | `docs/m9-real-company-validation-findings.md` §2–§3 — slots 6, 7, 9, 10 render on both real runs; both show "the interpretation call has not run" + the AI layer's `NOT CONFIGURED` note (HARD BOUNDS forbids a model call in the validation pass, not a defect here). | NOT YET — Calvin's gate. | IMPLEMENTED (content sourced; the interpretation/challenger call itself is unconfigured by design, not this row's gap). |
| **5. Full-analysis tabs** (#118 item 5) | Structure: `app/components/AnalyzerReport.test.tsx` — Sections A–J and six themed anchors (`business`, `financials`, `valuation`, `risks-thesis`, `market-context`, `evidence`) in contract order (cited `docs/m9-acceptance-record.md` §1). Content (Business + Market Context, the piece that was unbuilt through PR #180/#192): PR #200, `M9-ITEM5-CONTENT-01` (`5b2c728`, merged 2026-09-21T11:50:20Z). | `docs/m9-real-company-validation-findings.md` §2, §3 — "Full Analysis — Sections A–J intact, six themed anchors present" on both MSFT and OKLO. **CF-REALRUN-CURRENT-01, re-read at `2c805b4`:** Market Context now observed rendering the real `SEC classification (SIC ...)` line on both runs (never "Not yet available"); Business observed rendering the disclosed capture empty state on both (never the 10-K narrative — live-path only, §4 item 8); the verdict slot's evidence-status label (§8 item 1) observed absent on both, consistent with both runs' `INCOMPLETE` verdict. | Content source (not final live acceptance): Calvin's Option B ruling, [issue #195 comment](https://github.com/calyeap/Cal-Finance/issues/195#issuecomment-5759160247), 2026-09-21T10:39:55Z — approved the 10-K Item 1 narrative (Business) and SEC SIC/`sicDescription` classification (Market Context) as scoped sources. | IMPLEMENTED. |
| **6. Responsive tiers** (#118 item 6) | PR #165 `M9-RESPONSIVE-DESKTOP-01` (`baf2f54`, merged 2026-09-19T05:34:49Z) + PR #167 `M9-RESPONSIVE-COMPACT-01` (`e3da0be`, merged 2026-09-19T06:15:28Z) — `app/globalsCss.test.ts` (all five #118 tiers, three width-mode breakpoints, cited `docs/m9-acceptance-record.md` §2). | — the real MSFT/OKLO runs validated data/render correctness at default viewport, not breakpoint behaviour; breakpoint proof is the fixture-driven CSS test cited above, not a real-company run. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **7. Theme completion** (#118 item 7) | PR #169 `M9-THEME-COMPLETION-01` (`1ee3022`, merged 2026-09-19T07:02:48Z) — single light/dark toggle, shared Calboard chrome; light/dark parity structurally proved by `app/m9AcceptanceGaps.test.ts` (one `[data-theme="dark"]` rule, custom-property-only body; cited `docs/m9-acceptance-record.md` §1). | — theme parity is a stylesheet-structure proof, not something a real-company data run exercises. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **8. State handling** (#118 item 8) | PR #171 `M9-STATE-HANDLING-01` (`8f6f6a1`, merged 2026-09-19T07:32:59Z) — loading, stale, missing-evidence, suppression and failure/recovery states. | `docs/m9-real-company-validation-findings.md` §2–§3 — `LEVERAGE UNSUPPORTED IN v1`/`PASS`, `TRUST STATUS` `UNUSABLE`/`PARTIAL`, `PRECONDITION FAILED` (FCF yield), and four `NOT COMPUTED / SUPPRESSED` OKLO figures all observed rendering their cause on real data. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **9. Accessibility** (#118 item 9) | PR #174 `M9-ACCESSIBILITY-01` (`c381466`, merged 2026-09-19T08:24:21Z) + computed WCAG 2.1 contrast table, `app/m9AcceptanceGaps.test.ts` (cited `docs/m9-acceptance-record.md` §1: every ink/muted-on-ground/field/tint pair ≥ 4.5:1, both themes). | — contrast/keyboard/focus/colour-independence is a structural CSS/DOM proof, not something a real-company data run exercises differently. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **10. Real-company validation** (#118 item 10) | PR #178 `M9-REAL-COMPANY-VALIDATION-01` (`238b4fa`, merged 2026-09-19T09:49:30Z); updated by PR #190 `CF-S44-RECORD-01` (`a7e4a9c`, merged 2026-09-21T08:48:59Z) recording the §4.4 judgment. Tests: `app/components/analyzerSurfacesOnRealRun.test.tsx`, `lib/analyzer/m9RealCompanyValidationGuards.test.ts`, `lib/analyzer/nonOperatingJudgmentRecordedOnRealRun.test.ts`, `app/components/nonOperatingJudgmentRecordedOnRealRun.test.tsx`. | Is itself the real-company validation: `docs/m9-real-company-validation-findings.md`, MSFT and OKLO, acquired offline (`ANALYZER_OFFLINE=1`) from committed SEC captures, not fixtures. **Refreshed by CF-REALRUN-CURRENT-01 against `2c805b4`** — same two runs, same pipeline; §2/§3 gain the Market Context, Business and evidence-status-label observations row 5 now cites, and §4 gains one named gap (item 8, Business-on-capture). | §4.4 non-operating-investments classification: Calvin's `CALVIN DECISION`, [issue #188 comment 5757487226](https://github.com/calyeap/Cal-Finance/issues/188#issuecomment-5757487226), 2026-09-21T08:19:14Z — MSFT only (OKLO has zero tagged candidates). | IMPLEMENTED. Both runs still show verdict `INCOMPLETE` — expected per row 13, not a defect in this row. |
| **11. M9 acceptance** (#118 item 11, whole milestone) | PR #180 `M9-ACCEPTANCE-01` (`26b8de7`, merged 2026-09-19T10:25:59Z) — `docs/m9-acceptance-record.md`: visual, responsive, finance-logic, evidence/provenance, regression each independently **PASS**; `npx tsc --noEmit` clean; `npm test` 1743/1744 passing (the one failure is the pre-existing local Playwright chromium gap, proved non-reproducing in CI on PRs #169/#174/#178). | `docs/m9-real-company-validation-findings.md` is the fact set this acceptance pass checks against (row 10). | NOT YET — Calvin's gate. Explicitly not granted here (HARD BOUNDS). | **FAILS AS A WHOLE MILESTONE**, per `docs/m9-acceptance-record.md`'s own whole-milestone statement (reconciled by this outcome — see SCOPE item 6). Open: blocker 2 (row 13) and design-contract §8 items 2 and 4 (row 14). |
| **12. V1.5 presentation polish** (#118 item 12) | None — #118 authorises this "only after accepted M9"; row 11 shows M9 not yet accepted, so this item is not dependency-safe and has not started. | — not built. | NOT YET — Calvin's gate (blocked on row 11's live-product acceptance, which is his). | NOT STARTED — blocked on row 11. |
| **13. `deriveVerdict` / §10.6.2 growth comparator (M8)** — the dominant cause row 11 fails | No executable proof of a completed verdict path exists, and none is claimed: `deriveVerdict` (`lib/analyzer/verdict.ts:52-81`) returns `status: "INCOMPLETE"` on every one of its four branches, unconditionally, for every run — confirmed by direct read at `736e773`. The required second input, §10.6.2's required-versus-achieved growth comparator, is milestone M8 and has not been acquired (`verdict.ts:20-27`). | `docs/m9-real-company-validation-findings.md` §2 — MSFT's verdict cell: `INCOMPLETE`, reason quoted verbatim, "this verdict becomes available once M8 delivers the comparator fact." §3 — OKLO's verdict: `INCOMPLETE` (a different, earlier-branch cause; §10.6.2 is unreached for OKLO because its leverage suppression already returns `INCOMPLETE` first). | The 18 Sep 2026 ruling set the boundary rule this function implements (no manufactured HOLD; return `INCOMPLETE` with cause + recovery when decision-critical evidence is insufficient) — Calvin, [PR #140 comment 5719869973](https://github.com/calyeap/Cal-Finance/pull/140#issuecomment-5719869973) ("CALVIN DECISION RECEIPT") and its [reaffirmation, comment 5724006232](https://github.com/calyeap/Cal-Finance/pull/140#issuecomment-5724006232) ("ABSOLUTE AND FINAL unless he explicitly reverses it later", 2026-09-18T02:07:23Z) — the ruling issue #205 AUTHORITY calls the "FINAL OWNER RULING". Quoted in `lib/analyzer/verdict.ts:7-12`; implemented by PR #140 `CF-V2-PROOF-01` (`9780836`, merged 2026-09-18T03:30:48Z). That boundary rule is accepted; the M8 comparator itself is **NOT YET** acquired or accepted — a separate, later milestone. | **OPEN — blocked on M8.** Not this outcome's or blocker 2's to resolve (HARD BOUNDS). |
| **14. Design contract §8 items 2 and 4** — action-clause/verdict UI relationship; `INCOMPLETE`-default validation sequencing | `docs/design/m9-analyzer-design-contract.md` §8 items 2 and 4, **dispositioned by `CF-M9-BLOCKER14-RECON-01`, 24 Sep 2026**: item 2 — **ANSWERED**, Calvin's 21 Sep 2026 11:01:20Z ruling ([#196 comment 5759425335](https://github.com/calyeap/Cal-Finance/issues/196#issuecomment-5759425335)) keeps the §10.6.4 action clause (in `ScenarioRangeStrip`) separate/deferred from the top-level verdict (in `DominantVerdictSlot`); the V2 cutover keeps them as two separate components sharing only `az-hero` page adjacency (`app/components/AnalyzerReportFrame.tsx:105,112`), no shared component or cross-reference built. Item 4 — **NARROWED BY EVIDENCE**: the `INCOMPLETE`-by-default state is validated as first-class on the current V2 surface itself (below), not only on the legacy M9 routes; validating the `BUY`/`HOLD`/`SELL` branches stays blocked on M8, since they cannot occur before it. | `app/components/analyzerSurfacesOnRealRun.test.tsx` renders real MSFT and OKLO through `AnalyzerReportFrame` and asserts the shared hero shows the `INCOMPLETE` state, `verdict.reason`, and no confidence figure; `lib/analyzer/automaticAnalysisOnRealRun.test.ts` confirms both reach that result through the fully automatic pipeline; `lib/analyzer/m9RealCompanyValidationGuards.test.ts` pins `verdict.ts`'s `INCOMPLETE`-only content and guards the unified route against redirecting into a human step. Row 10/13 already show both real runs rendering `INCOMPLETE` with no action-clause/verdict merge attempted. | NOT YET — Calvin's gate. | **OPEN — dispositions recorded, nothing closed or accepted.** Items 1 and 3 of the same §8 list are RESOLVED (rows 5 and this file's reconciliation of `docs/m9-acceptance-record.md`); items 2 and 4 now each carry an evidenced disposition (answered / narrowed) rather than a bare "open," but neither is built, closed, or accepted here, and row 13 / condition 2 stay untouched (HARD BOUNDS). |
| **15. Normal-path Analyzer UX — `ticker → analyze → report`** (Calvin's 22 Sep 2026 04:28:04Z `CALVIN RULING`, PR [#216 comment 5771211284](https://github.com/calyeap/Cal-Finance/pull/216#issuecomment-5771211284); the blocker that refused the frontend for V1 freeze) | `CF-ANALYZER-AUTORUN-01` — `lib/analyzer/autoVerify.ts` (the rule by which the software confirms a fact itself), `lib/analyzer/autoRun.ts` (`advanceRunAutomatically`), `migrations/005_automatic_verification.sql` (the recorded origin), and the two route pages with their Screen 2 / Screen 3 redirects removed. Tests: `lib/analyzer/automaticAnalysisOnRealRun.test.ts`, `app/analyzer/analyzerRoutesOnRealRun.test.tsx`, `lib/analyzer/autoVerify.test.ts`, `lib/analyzer/m9RealCompanyValidationGuards.test.ts` (the inverted redirect guard). Frozen amendment: spec §14.8 (M9-1), scoped to the normal-path human requirement only; design contract §2.3. | `lib/analyzer/automaticAnalysisOnRealRun.test.ts` — real MSFT and real OKLO each reach a rendered analysis from `createRun` + `advanceRunAutomatically` with **no** human write, asserted against the stored rows (every decision `origin = AUTOMATIC`, no profile decision). Both companies' finance state is asserted **identical** to the same run worked by hand, and both still read `INCOMPLETE` for the row 13 reason. `app/analyzer/analyzerRoutesOnRealRun.test.tsx` renders all four routes on those runs with `redirect` mocked to throw. | NOT YET — Calvin's gate. His re-run of item (4) of the same ruling is what decides it; this row records that the pass is now possible, never that it was granted. | IMPLEMENTED. Row 11 is unaffected: this closes the 22 Sep frontend refusal, not blocker 2 (row 13) or design-contract §8 items 2/4 (row 14), which stay open and untouched. |

## What this file is not

Not a second acceptance record, not a PRD/TRD/TDD, not a new source of
truth, and not a ruling. It restates no finance methodology, threshold, or
verdict semantic — every cell either cites executable evidence re-read at
the SHA given, or states plainly that none exists and why. Where a cell
reads "NOT YET — Calvin's gate," that is the honest current state, not an
implicit request for a ruling this outcome is authorised to ask for; per
issue #205's own CALVIN REQUIRED section, none is expected from this
outcome.
