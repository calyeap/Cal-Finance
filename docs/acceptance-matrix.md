# Acceptance Matrix — current outcome → implementation proof → real-company validation → Calvin acceptance

**OUTCOME-ID:** `CF-ACCEPTANCE-MATRIX-01`, refreshed by `CF-ACCEPTANCE-MATRIX-REFRESH-01`
(issue [#314](https://github.com/calyeap/Cal-Finance/issues/314)). Finish-line
item (1) of Calvin's 21 Sep 2026 plan, per issue
[#202](https://github.com/calyeap/Cal-Finance/issues/202)'s closing
instruction. This file **records** current truth; it is not a source of
truth of its own. It subordinates itself to the authorities
[`docs/CURRENT-AUTHORITY.md`](CURRENT-AUTHORITY.md) already names, and to
[`AGENTS.md`](../AGENTS.md)'s precedence rule. It grants, claims or implies
nothing beyond what is cited: it does **not** grant Calvin's final
live-product acceptance of M9 or Analyzer V2, does not resolve blocker 2
(row 13, the M8 growth comparator) or design-contract §8 items 2/4 (row 14),
does not reopen the 18 Sep 2026 verdict-boundary ruling (row 13) or Calvin's
21 Sep rulings (rows 5, 10, and the confidence half of row 14's own
history), and does not promote NVDA's real run (row 10) into satisfaction of
finish-line item (2)'s third case.

Rows are #118's BOUNDED RUNWAY items 1–12 (issue
[#118](https://github.com/calyeap/Cal-Finance/issues/118), closed 21 Sep
2026 by PRs #169/#200 as a GitHub side-effect — its runway text still
governs per issue #205's AUTHORITY), plus the acceptance-relevant blockers
`docs/m9-acceptance-record.md` names that don't map to a single runway item,
plus rows this and prior refreshes added for outcomes that don't map onto
#118 at all. **Evidence re-read at `origin/master` = `3728479`** (2026-09-25,
`CF-LEASE-NESTING-LIMITATION-RECORD-01`, #313 — the head named by issue
#314). The prior header understated this: rows 14 and 15 were in fact
already current as of `b16edc2` (#283) and `e41ba71` (#218) respectively —
each PR updated its own row in place without bumping this header's SHA. This
refresh re-verifies every row against `3728479` directly (by opening the
cited file/line or running the cited test, not by trusting the prior
header) rather than assuming the gap was only cosmetic.
IMPLEMENTATION PROOF cites file/line, test name, merged-PR SHA, or CI —
never a PR body's own claim.

**What changed operationally since `2c805b4` that this refresh accounts
for**, in the order issue #314's table names them: the Analyzer V2 cutover
(`35ae6bf` #266 shell, `d35870b` #268 pre-report states, `4e14481` #285
cross-tab drift guard) — rows 3, 4, 5, 6, 7, 9, 14; the verdict-methodology
reconciliation chain (`c99712f` #271 → `ed81500` #273 → `2dda3e1` #275 →
`b77379c` #277 → `d847c68` #279 → `7a2dd69` #281) — row 13; the
design-contract §8 reconciliation (`b16edc2` #283) — row 14 (already
current, re-verified); the recorded analyst-input entry path and NVDA's
Step-7 bundle and real run (`519c4c6` #287, `f989c76`–`0753051` #289–#295,
`80ce12b` #297, `1d654a9` #299) and the two independent EV-input gates
(`c5f77c6` #301) — row 10; and five correctness/limitation reconciliations
that changed no acceptance state (`f740941` #303, `8909255` #305, `9ce3de6`
#307, `c01f57c` #310, `3728479` #313) — new row 16.

| OUTCOME | IMPLEMENTATION PROOF | REAL-COMPANY VALIDATION | CALVIN ACCEPTANCE | STATUS |
|---|---|---|---|---|
| **1. M9 contract reconciliation** (#118 item 1) | PR #149, `M9-CONTRACT-RECON-01` (`0a0f11c`, merged 2026-09-18T08:24:14Z) — `docs/design/m9-contract-reconciliation.md`, the verdict/failure/confidence boundary later runway items build from. Unaffected by anything merged since `2c805b4`; re-verified present and unedited at `3728479`. | — (documentation/reconciliation outcome; no product surface to run a real company against). | NOT YET — Calvin's gate. (The 15 Sep 2026 #118 authorisation covers the M9 initiative generally, not a per-item acceptance of this reconciliation.) | IMPLEMENTED. |
| **2. M9 design contract** (#118 item 2) | PR #152, `M9-DESIGN-CONTRACT-01` (`a770e30`, merged 2026-09-18T13:15:15Z) — `docs/design/m9-analyzer-design-contract.md`. Superseded as *implementation-facing* authority for Analyzer V2 by `docs/design/analyzer-v2-design-authority.md` (`CF-DESIGN-AUTHORITY-CUTOVER-01`, effective 23 Sep 2026 — `docs/CURRENT-AUTHORITY.md`), which states plainly that the M9 contract is now "legacy or surface-scoped evidence" and "cannot override" it; re-read at `3728479`, both files still present, the newer one's own text still makes exactly that statement. | — (design/IA document; nothing executable to validate against a real run). | NOT YET — Calvin's gate. | IMPLEMENTED as the historical M9 contract; superseded as current Analyzer V2 implementation authority (not reopened or re-litigated here). §8 leaves 4 items open — row 14 below tracks the two still-open ones, both now dispositioned. |
| **3. Desktop shell** (#118 item 3) | PR #159, `M9-DESKTOP-SHELL-01` (`830b058`) built one twelve-slot Overview page. **Restructured by `ANALYZER-V2-BUILD-01` (`35ae6bf`, #266), re-verified at `3728479`:** slots 1–4 and 12 now render inside the shared `AnalyzerReportFrame` shell (identity row, `az-hero` verdict/price/scenario block — `app/components/AnalyzerReportFrame.tsx`) plus `SourcesAndDetails` on the unified `/analyzer/[runId]` route, asserted by `AnalyzerReportFrame.test.tsx` and the cross-tab drift guard `app/components/analyzerV2AcceptanceTargets.test.tsx` (`CF-V2-DRIFT-GUARD-01`, `4e14481`, #285, pinning identity/hero/tab-rail/right-rail/body-start-anchor consistency at all (tab × acceptance-target-width) pairs). Slots 5–11 remain in `AnalyzerOverview.tsx`, now asserted by `AnalyzerOverview.test.tsx` as "slots 5-11, in order, direct children of `.ovtab`" (`:44-50`) — the original file's "all twelve slots, direct children of `main`" assertion no longer exists because the page it described no longer exists as one page; the slot content is not lost, it is redistributed and re-tested at its new location. The legacy single-page Overview PR #159 built is retired, not deleted-and-unproven. **The three pre-report states** (`ANALYZER-V2-PREREPORT-01`, `d35870b`, #268) were rebuilt on the same V2 shell in the same cutover — Analyzer Home (`.az-home`, one dominant ticker-entry action), Analyzing (`app/analyzer/[runId]/loading.tsx`, four locked stages), and True Failure (`error.tsx`/`not-found.tsx`, verified already-compliant, no changes needed) — none maps to a #118 runway item (they render before a report exists), so they are recorded here rather than left unaccounted for; no test regression from this outcome, confirmed by the same `npm test` run. | `docs/m9-real-company-validation-findings.md` §2–§3 — both MSFT and OKLO render all twelve slots' content, none absent, on the real acquired path (findings doc itself unedited since `e41ba71`, predates and is unaffected by the V2 restructuring, which moved *where* the content renders, not *whether* it is acquired or computed). `npm test` at `3728479` (2074/2075 passing, the one failure the known Playwright gap — see row 16) confirms both the legacy-shape and the current V2-shape tests pass simultaneously. | NOT YET — Calvin's gate. | IMPLEMENTED, at its new location. |
| **4. Overview implementation** (#118 item 4) | PR #161 `M9-OVERVIEW-CONTENT-01` (`909bc5b`) + PR #163 `M9-OVERVIEW-CHALLENGER-COPY-01` (`62d7dbb`) — slots 6–10 sourced editorial content. Restyled, not re-specified, by `ANALYZER-V2-BUILD-01` (`35ae6bf`, #266): the same slots (now numbered within `AnalyzerOverview.tsx`'s 5–11 range) carry the same editorial-content rule; `AnalyzerOverview.test.tsx` re-verified passing at `3728479`. | `docs/m9-real-company-validation-findings.md` §2–§3 — slots 6, 7, 9, 10 render on both real runs; both show "the interpretation call has not run" + the AI layer's `NOT CONFIGURED` note (HARD BOUNDS forbids a model call in the validation pass, not a defect here). | NOT YET — Calvin's gate. | IMPLEMENTED (content sourced; the interpretation/challenger call itself is unconfigured by design, not this row's gap). |
| **5. Full-analysis tabs** (#118 item 5) | Structure: `app/components/AnalyzerReport.test.tsx` still asserts "report order is exactly A, C, D, E, F, G, H, I, I2, B, J, at-a-glance" (`:69-74`) and the six themed anchors (`:75-80`), against the whole `AnalyzerReport` component — which, **since `ANALYZER-V2-BUILD-01` (`35ae6bf`, #266), backs only the snapshot-reopen route** (`app/analyzer/[runId]/snapshot/[version]/page.tsx:4,99`). On the live unified route (`app/analyzer/[runId]/page.tsx`), "Full Analysis" is no longer one scrolled page: the same six section functions `AnalyzerReport.tsx` exports (`BusinessSection`, `FinancialsSections`, `ValuationSections`, `RisksThesisSections`, `MarketContextSection`, `EvidenceSections`) each back one non-Overview tab of the seven-tab rail (`page.tsx`'s `TabBody`, `:41-69`), selected by `?tab=`. Same functions, same content rules, same tests (`businessMarketContextRendering.test.tsx`, `aiLayerRendering.test.tsx`, `suppressedRangeRendering.test.tsx` — all re-verified passing at `3728479`); presentation is tabbed, not scrolled — a legacy-surface note, not a content regression. Content (Business + Market Context): PR #200, `M9-ITEM5-CONTENT-01` (`5b2c728`), unaffected by the restructuring. | `docs/m9-real-company-validation-findings.md` §2, §3 — "Full Analysis — Sections A–J intact, six themed anchors present" on both MSFT and OKLO (via the legacy `AnalyzerReport` shape the snapshot route still exercises); Market Context renders the real `SEC classification (SIC ...)` line on both runs; Business renders the disclosed capture empty state on both (never the 10-K narrative — live-path only, §4 item 8); the verdict slot's evidence-status label observed absent on both, consistent with both runs' `INCOMPLETE` verdict. | Content source (not final live acceptance): Calvin's Option B ruling, [issue #195 comment](https://github.com/calyeap/Cal-Finance/issues/195#issuecomment-5759160247), 2026-09-21T10:39:55Z — approved the 10-K Item 1 narrative (Business) and SEC SIC/`sicDescription` classification (Market Context) as scoped sources. | IMPLEMENTED, at its new (tabbed) presentation. |
| **6. Responsive tiers** (#118 item 6) | PR #165 `M9-RESPONSIVE-DESKTOP-01` + PR #167 `M9-RESPONSIVE-COMPACT-01` — `app/globalsCss.test.ts` (all five #118 tiers, three width-mode breakpoints, still present and passing at `3728479`). **Extended by `CF-V2-DRIFT-GUARD-01` (`4e14481`, #285):** `app/components/analyzerV2AcceptanceTargets.test.tsx` (302 lines, new) pins the current Analyzer V2 design authority's own five required acceptance targets (32-inch desktop, half-window, iPad landscape, iPad portrait, iPhone) across every tab, combining the existing DOM-order technique with the existing CSS-source technique — the two breakpoint sets (the original #118 five tiers, and the current five V2 acceptance targets) coexist in the tree and both pass; neither superseded the other's test file. | — the real MSFT/OKLO runs validated data/render correctness at default viewport, not breakpoint behaviour; breakpoint proof is the fixture-driven CSS/DOM tests cited above, not a real-company run. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **7. Theme completion** (#118 item 7) | PR #169 `M9-THEME-COMPLETION-01` (`1ee3022`) — single light/dark toggle, shared Calboard chrome; light/dark parity structurally proved by `app/m9AcceptanceGaps.test.ts` (one `[data-theme="dark"]` rule, custom-property-only body), still present and passing at `3728479`. **Current authority narrows this row's live reach, named plainly rather than smoothed over:** `docs/design/analyzer-v2-design-authority.md` states "Analyzer V2 ships dark-only" and "no theme control in Analyzer V2." Confirmed in the tree — `app/components/AnalyzerTopBar.tsx:10-11,23` passes `showThemeToggle={false}` with the comment "Analyzer V2 ships dark-only and drops the theme control entirely"; `app/components/AnalyzerShell.tsx:14-15,59` hardcodes `data-theme="dark"` and "no longer reads `ThemeContext` or renders a theme toggle." Both Analyzer routes (`/analyzer/[runId]` and its snapshot route) use `AnalyzerShell`, so no live Analyzer UI control reaches the toggle today. The stylesheet-level parity structure PR #169 built is unremoved and still verified (`globals.css`'s single dark-mode rule, still custom-properties-only) — this is a current-authority direction change on *whether a control is exposed*, not a defect, deletion, or reopening of the parity proof itself. | — theme parity is a stylesheet-structure proof, not something a real-company data run exercises. | NOT YET — Calvin's gate. | IMPLEMENTED (stylesheet parity, historically and structurally, both re-verified). Live reachability of the toggle itself is superseded by current Analyzer V2 authority's own explicit direction — recorded as observed state, not resolved or re-opened here. |
| **8. State handling** (#118 item 8) | PR #171 `M9-STATE-HANDLING-01` (`8f6f6a1`) — loading, stale, missing-evidence, suppression and failure/recovery states. **Extended** by the price-honesty reconciliation trail (row 16): `CF-NOPRICE-HONESTY-RECON-01` (`f740941`, #303) and `CF-PRICE-DISPLAY-HONESTY-RECON-01` (`9ce3de6`, #307) added a new `NOT_COMPUTED_BINDING.price` state, bound in `ValuationStrip.tsx`, `QuickRead.tsx`, `AnalyzerReport.tsx`'s Section A row, `PriceChartPanel.tsx` and `AnalyzerReportFrame.tsx:76` — the same state-handling mechanism this row already covers, applied to one more honest-absence case (a run with no price), re-verified passing at `3728479`. | `docs/m9-real-company-validation-findings.md` §2–§3 — `LEVERAGE UNSUPPORTED IN v1`/`PASS`, `TRUST STATUS` `UNUSABLE`/`PARTIAL`, `PRECONDITION FAILED` (FCF yield), and four `NOT COMPUTED / SUPPRESSED` OKLO figures all observed rendering their cause on real data (MSFT/OKLO both have a price, so the new price-absence state is not exercised by either — see row 10's NVDA entry, which does). | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **9. Accessibility** (#118 item 9) | PR #174 `M9-ACCESSIBILITY-01` (`c381466`) + computed WCAG 2.1 contrast table, `app/m9AcceptanceGaps.test.ts`, re-verified present and passing at `3728479`: every ink/muted-on-ground/field/tint pair ≥ 4.5:1, both themes. `docs/design/analyzer-v2-design-authority.md`'s own shared colour-token table (`--ground`/`--ink`/`--muted`/`--field`, etc.) carries the identical hex values `globals.css` already used at M9; `--tint` is Analyzer-only and outside the shared table by that table's own design, unaffected. | — contrast/keyboard/focus/colour-independence is a structural CSS/DOM proof, not something a real-company data run exercises differently. | NOT YET — Calvin's gate. | IMPLEMENTED. |
| **10. Real-company validation** (#118 item 10) | PR #178 `M9-REAL-COMPANY-VALIDATION-01`; updated by PR #190 `CF-S44-RECORD-01` recording the §4.4 judgment. Tests: `app/components/analyzerSurfacesOnRealRun.test.tsx`, `lib/analyzer/m9RealCompanyValidationGuards.test.ts`, `lib/analyzer/nonOperatingJudgmentRecordedOnRealRun.test.ts`, `app/components/nonOperatingJudgmentRecordedOnRealRun.test.tsx` — all re-verified passing at `3728479`. **A third real company, NVDA, was run since `2c805b4`** via the recorded-bundle entry path `CF-ANALYST-INPUT-ENTRY-01` (`519c4c6`, #287) added (`lib/analyzer/acquisition/recordedBundles.ts`, migration `006_recorded_analyst_bundles.sql`), Calvin's approved Step-7/§6.3 bundle transcribed byte-faithful (`f989c76`–`0753051`, #289–#295), and the run itself opened through the same gated path MSFT/OKLO use (`CF-NVDA-RUN-OBSERVE-01`, `80ce12b`, #297) — `docs/nvda-realrun-observation.md`, reproduced by `lib/analyzer/nvdaRealRunObservation.test.ts` (still passing at `3728479`). | Is itself the real-company validation: `docs/m9-real-company-validation-findings.md`, MSFT and OKLO, acquired offline from committed SEC captures. **NVDA (third company, `docs/nvda-realrun-observation.md`), recorded at its own stated weight — not as satisfaction of finish-line item (2)'s third case, which the 21 Sep 2026 deferral ([#209 comment 5763269897](https://github.com/calyeap/Cal-Finance/issues/209#issuecomment-5763269897)) left to Calvin, and which the observation document's own text disclaims ("marks no acceptance-matrix row satisfied").** NVDA's own results: price is the honest `{value:0, timestamp:""}` sentinel (no `NVDA` row in the committed `prices.json`); enterprise value `INCOMPLETE` on three REQUIRED inputs (`treasuryMethodDilution`, `financeLeaseLiabilities`, `nonOperatingEquityInvestmentsAtBook` — the §4.4 judgment left deliberately unmade, HARD BOUNDS); leverage `LEVERAGE UNSUPPORTED IN v1`; fair-value range suppressed; trust `UNUSABLE`; verdict `INCOMPLETE`. The two non-§4.4 EV gaps were separately reconciled against authority (`CF-EV-INPUT-GAPS-RECON-01`, `c5f77c6`, #301, `docs/ev-input-gaps-reconciliation.md`) and both end as **named gates** (`treasuryMethodDilution` needs acquisition machinery outside this line of work's HARD BOUNDS; `financeLeaseLiabilities` has no second mapped candidate tag for this filer) — not acquired, not `CALVIN REQUIRED`. NVDA's own RONIC ladder, previously blocked pipeline-wide, is now CLEAN at 75.24% (`CF-RONIC-DELTAS-RECON-01`, `1d654a9`, #299 — row 13 cites the verdict-relevant consequence). NVDA closes both of §12's named minimum-evidence gaps (a RONIC ladder not uniformly NOT MEANINGFUL; both a ten-year and five-year achieved comparator on one series) — reported as evidence only, per that document's own text; it "concludes nothing about which way any §11 open item should be ruled." | §4.4 non-operating-investments classification (MSFT only): Calvin's `CALVIN DECISION`, [issue #188 comment 5757487226](https://github.com/calyeap/Cal-Finance/issues/188#issuecomment-5757487226). NVDA's own §4.4 judgment is explicitly **unmade** (HARD BOUNDS of `CF-NVDA-RUN-OBSERVE-01`) — open, not this outcome's, not this refresh's. | IMPLEMENTED for MSFT/OKLO. NVDA IMPLEMENTED as a third recorded real-company run with its verdict still `INCOMPLETE` — expected per row 13, not a defect; NVDA's inclusion here is evidentiary weight only, per the observation document's own disclaimer, and does not itself close finish-line item (2)'s third case. |
| **11. M9 / Analyzer V2 acceptance** (#118 item 11, whole milestone) | PR #180 `M9-ACCEPTANCE-01` — `docs/m9-acceptance-record.md` (byte-unchanged per HARD BOUNDS; re-read in full at `3728479`, its own text already tracks every disposition through `CF-M9-BLOCKER14-RECON-01`): visual, responsive, finance-logic, evidence/provenance, regression each independently **PASS**. **Refreshed under the current controlling ruling — `CALVIN RULING — SUPERSEDE OPTION B FOR ANALYZER V2 FINAL ACCEPTANCE`** ([issue #210 comment 5807320616](https://github.com/calyeap/Cal-Finance/issues/210#issuecomment-5807320616), 2026-09-24T03:58:03Z): do not accept Analyzer V2 as complete while a successful real run's dominant verdict remains `INCOMPLETE`; do not revert the merged V2 UI or reopen its locked design; the verdict/completed-report dependency is the next Analyzer finish-line dependency. **The blocking set at head, stated from evidence, not carried forward:** (a) row 13's verdict/M8 dependency — `deriveVerdict` still returns `INCOMPLETE` unconditionally on every branch for every run (`lib/analyzer/verdict.ts:61-109`, five branches, re-read in full), and §11's seven house-policy items and §12's minimum-evidence composition requirement in `docs/verdict-methodology-reconciliation.md` remain open (one item, item 8, closed by `CALVIN RULING — B`; the rest — items 1,2,3,4,5,6,7 — still open); (b) design-contract §8 items 2 and 4 (row 14) — both now **dispositioned** (ANSWERED / NARROWED BY EVIDENCE, `CF-M9-BLOCKER14-RECON-01`) but neither closed, built, or accepted. Both (a) and (b) are unchanged in kind by everything else merged since `2c805b4` — the correctness reconciliations in row 16 are confirmed latent/byte-identical and change no output; NVDA (row 10) adds a third `INCOMPLETE` real run, which the controlling ruling's own text treats as the condition acceptance is withheld under, not evidence toward lifting it. **Final acceptance of Analyzer V2 (or of M9 as a whole milestone) is stated plainly here as withheld, and is Calvin's — nowhere granted, claimed, or implied by this refresh.** `docs/m9-acceptance-record.md` was re-read in full at `3728479` (byte-unchanged, HARD BOUNDS) looking for a point falsified at head: none of its dimension-level PASS findings or its whole-milestone statement is contradicted by the current tree — the one place current reality has moved past its framing is row 7's point (the light/dark toggle its §1 Visual PASS assumed live-reachable is no longer exposed by any Analyzer route under current V2 authority), named there with evidence rather than here, since it is a presentation-authority change, not a falsification of the record's own PASS finding (the underlying CSS parity structure it tested still holds, unedited). | `docs/m9-real-company-validation-findings.md` (MSFT/OKLO) and `docs/nvda-realrun-observation.md` (NVDA, row 10) are the fact set this acceptance pass checks against — three real runs, three `INCOMPLETE` verdicts. | NOT YET — Calvin's gate. Explicitly not granted here (HARD BOUNDS), under either the pre-V2 #118 framing or the current V2 controlling ruling. | **FAILS AS A WHOLE MILESTONE / WITHHELD FOR ANALYZER V2**, per `docs/m9-acceptance-record.md`'s own whole-milestone statement and the 24 Sep 2026 controlling ruling. Open: row 13 (blocked on M8) and design-contract §8 items 2/4 (row 14, dispositioned but not closed). |
| **12. V1.5 presentation polish** (#118 item 12) | None — #118 authorises this "only after accepted M9"; row 11 shows M9/Analyzer V2 not yet accepted, so this item is not dependency-safe and has not started. Unaffected by anything merged since `2c805b4`. | — not built. | NOT YET — Calvin's gate (blocked on row 11's live-product acceptance, which is his). | NOT STARTED — blocked on row 11. |
| **13. `deriveVerdict` / §10.6.2 growth comparator (M8)** — the dominant cause row 11 fails | **`lib/analyzer/verdict.ts:61-109`, re-read in full at `3728479`, now five unconditional branches** (grew from four since the last full re-read): `trust.status === "UNUSABLE"` → `INCOMPLETE` (`:62-68`); `range.kind === "suppressed"` → `INCOMPLETE` (`:72-77`); `range.kind === "pre-revenue-distribution"` → `INCOMPLETE` (`:79-84`); **new** — `PROFILE NOT CONFIRMED` active on `trust.determinedBy` → `INCOMPLETE`, named explicitly (`:93-103`, added by `CF-VERDICT-NONPOLICY-GAPS-01`, `7a2dd69`, #281, closing `docs/verdict-methodology-reconciliation.md` §6.3/§13's standing implementation gap); otherwise → `INCOMPLETE`, `COMPARATOR_NOT_YET_AVAILABLE` (`:105-108`). No branch computes a position of any kind; `lib/analyzer/policy.ts` re-read in full — `PolicyConstants` and `POLICY_THRESHOLD_PROVENANCE` still carry no §10.6.2 entry of any kind. **The frozen spec's §10.6.2 text itself has changed and this code has not**, which is the precise current state, not a contradiction: the verdict-methodology reconciliation chain (`docs/verdict-methodology-reconciliation.md`, `CF-VERDICT-SYNTHESIS-RND-01` `c99712f` #271 → `CF-VERDICT-METHODOLOGY-RECON-01` `ed81500` #273 → `CF-VERDICT-1062-AMEND-TEXT-01` `2dda3e1` #275) reached `CALVIN RULING — C`'s seven-step derivation (underwritability → growth licence → value → margin of safety → verdict → flip test → reverse-DCF-as-explanation-only), applied to the frozen spec by `CF-VERDICT-1062-APPLY-01` (`b77379c`, #277, §10.6.2) and `CF-VERDICT-1064-APPLY-01` (`d847c68`, #279, §10.6.4's dependent wording) — both frozen-text-only, `docs/frozen/calboard-stock-analyzer-v1-spec.md` amended, `lib/analyzer/verdict.ts`/`policy.ts` untouched by either PR (each PR's own diffstat confirms zero `lib/` lines touched). §11's eight re-derived house-policy decisions in the reconciliation doc are unfilled except item 8 (closed by `CALVIN RULING — B`); none is answered by this refresh (HARD BOUNDS). **RONIC is no longer the blocker it once was for either calibration company**, closed by `CF-RONIC-DELTAS-RECON-01` (`1d654a9`, #299): MSFT's RONIC ladder is now CLEAN at 17.5% (`lib/analyzer/reverseDcfOnRealRun.test.ts:80`, was uniformly NOT MEANINGFUL at the prior read), NVDA's is CLEAN at 75.24% (row 10) — but this closes only one of §12's composition-requirement bullets; it does not itself construct a comparator, and does not change that `deriveVerdict` still has no code path that reads either dimension of the ruled structure. | `docs/m9-real-company-validation-findings.md` §2 — MSFT's verdict cell: `INCOMPLETE`, reason quoted verbatim. §3 — OKLO's verdict: `INCOMPLETE` (a different, earlier-branch cause — leverage suppression). `docs/nvda-realrun-observation.md` — NVDA's verdict: `INCOMPLETE`, cause `LEVERAGE UNSUPPORTED IN v1` (cascaded from the unmade §4.4 judgment plus the two named EV gates, row 10) — NVDA never reaches this row's §10.6.2 question either, for a different upstream reason than OKLO's. | The 18 Sep 2026 FINAL OWNER RULING set the boundary rule this function implements (no manufactured HOLD; return `INCOMPLETE` with cause + recovery) — quoted in `verdict.ts:7-13`; that boundary rule is accepted and unreopened. **`CALVIN RULING — C`** ([PR #273 comment 5808704819](https://github.com/calyeap/Cal-Finance/pull/273#issuecomment-5808704819), 24 Sep 2026) and **`CALVIN RULING — B`** ([PR #275 comment 5809629191](https://github.com/calyeap/Cal-Finance/pull/275#issuecomment-5809629191) / [PR #279 comment 5810709453](https://github.com/calyeap/Cal-Finance/pull/279#issuecomment-5810709453)) have ruled on the derivation's *shape* and applied its *text* to the frozen spec; §11's *numeric/house-policy* content (items 1,2,3,4,5,6,7) is **NOT YET** ruled, and no code implementing even the shape exists yet — a separate, later outcome. | **OPEN — blocked on M8's comparator and on §11's remaining house-policy items.** The frozen-text amendment (rows above) narrows what M8 needs to build; it does not itself build it. Not this outcome's or row 11's to resolve (HARD BOUNDS). |
| **14. Design contract §8 items 2 and 4** — action-clause/verdict UI relationship; `INCOMPLETE`-default validation sequencing | `docs/design/m9-analyzer-design-contract.md` §8 items 2 and 4, dispositioned by `CF-M9-BLOCKER14-RECON-01` (`b16edc2`, #283), **re-verified unchanged and still current at `3728479`**: item 2 — **ANSWERED**, Calvin's 21 Sep 2026 11:01:20Z ruling ([#196 comment 5759425335](https://github.com/calyeap/Cal-Finance/issues/196#issuecomment-5759425335)) keeps the §10.6.4 action clause (`ScenarioRangeStrip`) separate/deferred from the top-level verdict (`DominantVerdictSlot`); both remain two separate components sharing only `az-hero` page adjacency (`app/components/AnalyzerReportFrame.tsx:108,115`, re-read at current line numbers — small drift from `:105,112` at the prior read, same relationship, no shared component or cross-reference built). Item 4 — **NARROWED BY EVIDENCE**: the `INCOMPLETE`-by-default state is validated as first-class on the current V2 surface itself, not only the historical M9 routes; validating `BUY`/`HOLD`/`SELL` stays blocked on M8 (row 13). | `app/components/analyzerSurfacesOnRealRun.test.tsx` renders real MSFT and OKLO through `AnalyzerReportFrame` and asserts the shared hero shows `INCOMPLETE`, `verdict.reason`, and no confidence figure — re-verified passing at `3728479`; `lib/analyzer/automaticAnalysisOnRealRun.test.ts` and `lib/analyzer/m9RealCompanyValidationGuards.test.ts` corroborate, both re-verified passing. NVDA (row 10) is a third real run reaching the same `INCOMPLETE` hero state through the same unified route, though not yet run through this specific test file. | NOT YET — Calvin's gate. | **OPEN — dispositions recorded, nothing closed or accepted.** Unchanged since `b16edc2`; this refresh confirms the disposition still holds against the current tree rather than merely repeating it. |
| **15. Normal-path Analyzer UX — `ticker → analyze → report`** (Calvin's 22 Sep 2026 04:28:04Z `CALVIN RULING`, PR [#216 comment 5771211284](https://github.com/calyeap/Cal-Finance/pull/216#issuecomment-5771211284)) | `CF-ANALYZER-AUTORUN-01` — `lib/analyzer/autoVerify.ts`, `lib/analyzer/autoRun.ts` (`advanceRunAutomatically`), `migrations/005_automatic_verification.sql`. Tests: `lib/analyzer/automaticAnalysisOnRealRun.test.ts`, `app/analyzer/analyzerRoutesOnRealRun.test.tsx`, `lib/analyzer/autoVerify.test.ts`, `lib/analyzer/m9RealCompanyValidationGuards.test.ts` — all re-verified passing at `3728479`. **The unified route (`app/analyzer/[runId]/page.tsx`) now calls `advanceRunAutomatically` directly** (`ANALYZER-V2-BUILD-01`, `35ae6bf`, #266) in place of the retired separate `/report` route, which now only redirects (`app/analyzer/[runId]/report/page.tsx`, `redirect(/analyzer/${runId}?tab=business)`) — same automatic-advance behaviour, one fewer route. | `lib/analyzer/automaticAnalysisOnRealRun.test.ts` — real MSFT and real OKLO each reach a rendered analysis with **no** human write, every decision `origin = AUTOMATIC`. NVDA (row 10) reaches its `INCOMPLETE` result through the same recorded-bundle path plus this same automatic pipeline, per `docs/nvda-realrun-observation.md`'s own description of the run it opened. | NOT YET — Calvin's gate. His re-run of item (4) of the same ruling is what decides it. | IMPLEMENTED. Row 11 is unaffected: this closes the 22 Sep frontend refusal, not blocker 2 (row 13) or design-contract §8 items 2/4 (row 14). |
| **16. Post-`2c805b4` correctness & known-limitation reconciliations (non-blocking maintenance)** — not a #118 item; recorded so no merged outcome is silently dropped | Five outcomes, each verified latent or purely record-keeping — none changes any row above's blocking state: **(a)** `CF-NOPRICE-HONESTY-RECON-01` (`f740941`, #303, `docs/noprice-honesty-reconciliation.md`) — carries a real, possibly-absent price through `computeEnterpriseValue`'s REQUIRED check and `scenarioOutputs.priceLocationWithinRange` instead of a flattened `$0`; MSFT/OKLO outputs byte-identical, no `INCOMPLETE` removed. **(b)** `CF-MULTIPLES-NOPRICE-RECON-01` (`8909255`, #305, `docs/multiples-noprice-reconciliation.md`) — same class of fix for `multiplesInput.price` and two further reads; same byte-identical confirmation. **(c)** `CF-PRICE-DISPLAY-HONESTY-RECON-01` (`9ce3de6`, #307, `docs/price-display-honesty-reconciliation.md`) — closes the presentation layer (row 8 cites the resulting state binding directly). **(d)** `CF-EV-BRIDGE-PERIOD-COHERENCE-IMPL-01` (`c01f57c`, #310, `docs/ev-bridge-period-coherence-reconciliation.md`) — requires the §3.5 EV bridge's three instant balance-sheet inputs to share one as-of date; "latent-only for every company this repo can currently run," per its own commit message, confirmed by `lib/analyzer/modules/enterpriseValue.test.ts`, re-verified passing. **(e)** `CF-LEASE-NESTING-LIMITATION-RECORD-01` (`3728479`, #313, `docs/ev-lease-nesting-doublecount-reconciliation.md`) — lands **`CALVIN RULING — NONE FOR NOW`** ([#311 comment 5826854441](https://github.com/calyeap/Cal-Finance/issues/311#issuecomment-5826854441)): the finance-lease nesting double-count (sized $105M on UNP; MSFT/OKLO `NOT NESTED`, NVDA `UNKNOWN`, $0 overstatement on the three currently-real-run-capable captures) stays a documented known limitation for the Analyzer V2 finish line, not corrected; "proceed to the remaining Analyzer acceptance/closeout work." | Each outcome's own re-run against the real MSFT/OKLO/NVDA captures shows no output change from before its own fix — the honest classification changed (a hazard is now named or a flattening is now a suppressed state), not any company's price, verdict, or figure. No new real-company run was performed by any of the five. | N/A for (a)–(d) — engine-honesty/correctness fixes, not acceptance decisions, none `CALVIN REQUIRED`. (e) carries `CALVIN RULING — NONE FOR NOW` — landed as a documented limitation, not an acceptance grant of anything downstream, and its own text names the two conditions (blocks a real supported company; a separately-scoped post-freeze reliability improvement) under which it is revisited, neither evaluated here. | IMPLEMENTED (each individually merged and tested at `3728479`; zero effect on rows 10, 11 or 13's blocking state). |

## What this file is not

Not a second acceptance record, not a PRD/TRD/TDD, not a new source of
truth, and not a ruling. It restates no finance methodology, threshold, or
verdict semantic — every cell either cites executable evidence re-read at
the SHA given, or states plainly that none exists and why. Where a cell
reads "NOT YET — Calvin's gate," that is the honest current state, not an
implicit request for a ruling this outcome is authorised to ask for; per
issue #205's own CALVIN REQUIRED section and issue #314's own CALVIN
REQUIRED section, none is expected from this outcome or this refresh.
